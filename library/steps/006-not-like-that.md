---
name: NotLikeThat
description: Not like that, it won't work
enabled: true
---

Assume `contextual_data`, `instructions_definition`, and `goals_success_definition` as the governing frame.
Select as many elements as needed from session_payload —such as the text, title and subheadings, length, pace, flow, content, structure, readability, or specific parts of it—and, for each selected element:
Say "Not like that, it won't work" and elaborate:
Say "This is what will work" and elaborate:

Apply the preceding elaborations to the current session_payload: replace the identified elements with what will work, preserve unaffected material, and produce one complete reformulated session_payload.
Populate and return the full, modified state in StepResult.session_payload.
